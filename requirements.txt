# ─────────────────────────────────────────────────────────
#  OA Gait Analysis — Python Dependencies
#  All packages run fully offline after initial pip install.
#  Tested on Python 3.9 – 3.11
#  Production release: CareSetu v1.0
# ─────────────────────────────────────────────────────────

# Python version constraint (MediaPipe doesn't officially support 3.13 yet)
#python>=3.9,<3.13

# Pose estimation (includes bundled TFLite models — no internet needed at runtime)
mediapipe==0.10.8

# Computer vision & video I/O
opencv-python==4.8.1.78

# Numerical / linear algebra (joint angle calculations)
numpy==1.24.3

# Type hints compatibility
typing-extensions>=4.5.0
